/** @type {import('tailwindcss').Config} */
export default {
  content: [
    "./src/**/*.{js,jsx,ts,tsx}", // This tells Tailwind to scan all JS, JSX, TS, and TSX files in the src folder
  ],
  theme: {
    extend: {}, // Use this to add custom themes or colors if needed
  },
  plugins: [],
}
